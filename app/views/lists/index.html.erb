<div class="container">
  <h1>Mes listes</h1>
  <h2>Hello <%= current_user.username %> 👋 </h2>
  <p>Vous avez <%= current_user.lists.count %> listes.</p>
  <div class="mt-4 mb-5">
    <%= link_to "Créer une liste", new_list_path, class: "btn btn-primary" %>
  </div>
  <% unless @lists.empty? %>
    <div class="row">
    <% @lists.each do |list| %>
      <%= render "lists/partial/list-card", list: list %>
    <% end %>
  <% end %>
  </div>
</div>
